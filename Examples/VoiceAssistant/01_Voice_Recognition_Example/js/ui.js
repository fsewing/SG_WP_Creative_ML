function drawLegend() {
  fill(225);
  textSize(12);

  text(
    "Click once to allow access to the microphone (if needed)",
    20,
    windowHeight - 40
  );
  text(
    'Speak in either: "Rechteck", "Kreis" or "Dreieck"',
    20,
    windowHeight - 20
  );
}
